# Local Development Environment (Minikube)

# Service Flags
redis:
  enabled: true

minio:
  enabled: true

postgres:
  enabled: false  # Optional

monitoring:
  enabled: true

api:
  enabled: true  # Phase 3

frontend:
  enabled: true  # Phase 3

ingress:
  enabled: false  # Minikube uses its own ingress addon

# Global Settings
global:
  environment: local
  domain: vision.local
