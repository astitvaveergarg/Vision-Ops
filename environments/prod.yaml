# Production Environment (AWS EKS / Cloud Kubernetes)

# Service Flags
redis:
  enabled: true

minio:
  enabled: true

postgres:
  enabled: false

monitoring:
  enabled: true

argocd:
  enabled: true

api:
  enabled: true

frontend:
  enabled: true

ingress:
  enabled: true

# Global Settings
global:
  environment: prod
  domain: vision.example.com
