# Development Environment (Cloud Kubernetes Cluster)

# Service Flags
redis:
  enabled: true

minio:
  enabled: true

postgres:
  enabled: false

monitoring:
  enabled: true

api:
  enabled: true

frontend:
  enabled: true

ingress:
  enabled: true
  staticIP: "35.194.30.3"  # Reserved static IP - never changes!

# Global Settings
global:
  environment: dev
  domain: vision-ops.xyz
